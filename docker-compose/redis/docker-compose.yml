services:
  redis:
    image: redis:7-alpine
    container_name: egov-redis
    restart: unless-stopped
    ports:
      - "${REDIS_PORT:-6379}:6379"
    command:
      - redis-server
      - --requirepass
      - "${REDIS_PASSWORD:-rhdxhd12}"